%div.container-fluid
  %div.row
    %div.col-xs-2
    %div.col-xs-8
      -if @tweets.count > 0
        -@tweets.order_latest.each do |tweet|
          %div.card-block
            %div.card
              %div.card-header
                =link_to tweet.user.name, user_path(tweet.user.id)
              %div.card-text
                #{tweet.body}
              %div.card-footer
      -else
        %h2 合致するツイートとアカウント名がありませんでした。
