%h1 Listing following

%table
  %thead
    %tr
      %th User
      %th Following user
      %th
      %th
      %th

  %tbody
    - @following.each do |following|
      %tr
        %td= following.user_id
        %td= following.following_user_id
        %td= link_to 'Show', following
        %td= link_to 'Edit', edit_following_path(following)
        %td= link_to 'Destroy', following, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Following', new_following_path
